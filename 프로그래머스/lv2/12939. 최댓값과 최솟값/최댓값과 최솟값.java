class Solution {
    public String solution(String s) {
        
        String [] arr = s.split(" ");
        
        for(int i = 0 ; i < arr.length ; i++){
            int [] num = Integer.parseInt(arr[i]);
        }
        String answer = "";
        return answer;
    }
}